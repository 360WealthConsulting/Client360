"""Canonical document publication — the explicit grant from a ``documents`` row to a client audience.

See :mod:`app.services.publication.service` for the mutation API, :mod:`app.services.publication.policy`
for the type-to-visibility policy, and :mod:`app.services.publication.preview` for the read-only corpus
preview (which proposes and never publishes).
"""
