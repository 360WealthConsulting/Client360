# Client360 Architecture Decision Records (ADRs)

## Purpose
ADRs preserve the **reasoning** behind Client360's major architectural decisions — the
problem that existed, the decision made, the alternatives rejected, the consequences, the
constraints future phases must preserve, and the conditions under which a decision may be
revisited. They exist so future development does not have to reverse-engineer *why* the current
design is the way it is.

All ADRs in this set reflect the architecture **as implemented after Phase D.12A** on
`release/0.13.0` (migration head `j0b1u2s3o4w5`, 352 routes). They describe implemented
decisions, not aspirations.

## Three documentation layers (do not conflate)
- **`docs/PLATFORM_ARCHITECTURE.md`** — *what exists now* (authoritative top-level reference:
  domains, source-of-truth matrix, capabilities, scope, redaction, boundaries).
- **`docs/adr/`** (this directory) — *why the key decisions were made* (durable rationale).
- **`docs/PHASE_D*.md` / `docs/ADVISOR_WORKSPACE_ARCHITECTURE.md`** — *phase-specific history*
  (how each slice was designed and delivered). These remain historical and are not rewritten.

## ADR numbering rules
- ADRs are numbered sequentially: `ADR-001`, `ADR-002`, … (zero-padded to three digits).
- Filenames: `docs/adr/ADR-NNN-short-kebab-title.md`.
- Numbers are **never reused**. A number, once assigned, belongs to that decision forever
  (even if later superseded).
- A new decision that replaces an old one gets a **new** number and marks the old one
  `Superseded` (see below).

## Status definitions
- **Accepted** — the decision is in force and reflected in the current codebase.
- **Superseded** — replaced by a later ADR (which is named in the `Superseded_by` note). The
  superseded ADR is kept as a historical record and is **not** deleted or rewritten.
- **Deprecated** — no longer recommended but not yet replaced; retained for context.
- **Proposed** — under consideration, not yet in force. (No ADR in this set is `Proposed`.)

## How to propose a new ADR
1. Copy the structure of an existing ADR (headings below are mandatory).
2. Assign the next unused sequential number.
3. Set `Status: Proposed` while under review; change to `Accepted` on approval.
4. Fill every required section with **evidence from the code** (files, migrations, tests).
5. Add a row to the table below and link it.
6. Update `docs/PLATFORM_ARCHITECTURE.md` and, where hard facts change,
   `docs/platform_architecture_manifest.yaml` and the relevant tests.

## How to supersede an ADR
- Do **not** edit the accepted ADR's decision. Create a **new** ADR that states it supersedes
  the prior one; add `Superseded_by: ADR-NNN` to the old ADR's Status section and set its
  status to `Superseded`. Accepted ADRs are historical records — only typographical fixes that
  do not alter the decision are permitted in place.

## Required ADR headings
Every ADR must contain, in order: `# ADR-NNN — Title`, `## Status`, `## Date`,
`## Decision owners`, `## Context`, `## Decision`, `## Alternatives considered`,
`## Reasons for the decision`, `## Consequences` (with `### Positive consequences` and
`### Negative consequences and tradeoffs`), `## Enforcement`, `## Exceptions`,
`## Revisit conditions`, `## References`.

## Decision ownership and approval
ADR **acceptance is architectural approval, not automatically regulatory approval.**
- General technical ADRs require: **Platform Architecture**, the relevant **Domain Owner**, and
  the **Business Operations Owner**.
- Compliance-related ADRs (ADR-008, and any future ADR affecting regulated rule sets or
  approval authority) additionally require an **authorized Compliance reviewer**. Where the
  repository does not yet name one, ADRs state *"Authorized compliance reviewer: Not yet
  designated"* and mark any future regulated rule change as requiring compliance sign-off.
- **Michael Shelton** may be listed as **Business Operations Owner** for workflow and
  operational requirements. His business approval is **not** regulatory certification.

## Architecture change process
1. Determine whether a change affects an **Accepted** ADR.
2. If not, implement under the existing architecture.
3. If it does, do **not** silently edit the accepted ADR.
4. Create a new ADR that **supersedes** the prior one or **records an approved exception**.
5. Update `PLATFORM_ARCHITECTURE.md`, `platform_architecture_manifest.yaml` (where hard facts
   change), relevant tests, and phase documentation.
6. Obtain required architectural (and, where applicable, compliance) approvals.
7. Merge only after CI passes.

## Reviewer expectations
Reviewers confirm that each ADR: reflects **implemented** code (not aspiration); cites
repository-relative evidence; keeps composition layers non-authoritative; keeps regulatory
approval inside authorized Compliance; and does not rewrite prior phase history.

## ADR index

| # | Title | Status | Decision area | Related phase |
|---|-------|--------|---------------|---------------|
| [ADR-001](ADR-001-composition-layers.md) | Composition layers | Accepted | Platform structure | D.2–D.12 |
| [ADR-002](ADR-002-domain-ownership-and-source-of-truth.md) | Domain ownership and source of truth | Accepted | Platform structure | D.2–D.12A |
| [ADR-003](ADR-003-relationship-entity-and-business-ownership-graph.md) | Relationship-entity & business-ownership graph | Accepted | Identity/relationships | D.12 |
| [ADR-004](ADR-004-server-side-authorization-and-record-scope.md) | Server-side authorization & record scope | Accepted | Security | D.1–D.12 |
| [ADR-005](ADR-005-sensitive-data-redaction-and-restricted-vs-missing.md) | Sensitive-data redaction; restricted vs missing | Accepted | Security | D.10–D.12 |
| [ADR-006](ADR-006-advisor-intelligence-as-deterministic-computation.md) | Advisor Intelligence as deterministic computation | Accepted | Intelligence | D.5 |
| [ADR-007](ADR-007-advisor-work-as-owned-work-management-not-workflow-engine.md) | Advisor Work: work management, not workflow engine | Accepted | Work | D.9 |
| [ADR-008](ADR-008-compliance-decision-and-reviewer-authority-boundaries.md) | Compliance decision & reviewer-authority boundaries | Accepted | Compliance | D.6–D.8 |
| [ADR-009](ADR-009-activity-timeline-as-projection-not-event-sourced-platform.md) | Activity Timeline as projection, not event sourcing | Accepted | Events | D.10 |
| [ADR-010](ADR-010-annual-review-as-meeting-oriented-composition.md) | Annual Review as meeting-oriented composition | Accepted | Composition | D.11 |
| [ADR-011](ADR-011-business-owner-planning-as-business-planning-composition.md) | Business Owner Planning as business-planning composition | Accepted | Composition | D.12 |
| [ADR-012](ADR-012-business-planning-profile-as-narrow-prospective-persistence.md) | Business-planning profile: narrow prospective persistence | Accepted | Persistence | D.12 |
| [ADR-013](ADR-013-additive-read-services-belong-to-owning-domains.md) | Additive reads belong to owning domains | Accepted | Service design | D.11–D.12 |
| [ADR-014](ADR-014-no-mutation-during-incidental-rendering.md) | No mutation during incidental rendering | Accepted | Integrity | D.12 |
| [ADR-015](ADR-015-no-fabricated-data-history-calculations-or-relationships.md) | No fabricated data, history, calculations, or relationships | Accepted | Integrity | D.5–D.12 |
| [ADR-016](ADR-016-linear-migration-chain-and-declared-schema-registration.md) | Linear migration chain & declared-schema registration | Accepted | Database | D.6–D.12 |
| [ADR-017](ADR-017-architecture-manifest-and-enforcement-tests.md) | Architecture manifest & enforcement tests | Accepted | Governance | D.12A |
| [ADR-018](ADR-018-opportunity-pipeline-as-authoritative-domain.md) | Opportunity & Pipeline as an authoritative domain | Accepted | Domain (business development) | D.13 |
| [ADR-019](ADR-019-campaigns-referral-sources-and-attribution.md) | Campaigns, Referral Sources & attribution | Accepted | Domain (business development) | D.14 |
| [ADR-020](ADR-020-analytics-as-read-model.md) | Enterprise Analytics as a deterministic read-model | Accepted | Domain (analytics) | D.15 |
| [ADR-021](ADR-021-document-platform-as-authoritative-domain.md) | Document Management as the authoritative artifact domain | Accepted | Domain (documents) | D.16 |
| [ADR-022](ADR-022-workflow-orchestration-layer.md) | Workflow Automation as an orchestration layer over the existing engine | Accepted | Domain (workflow) | D.17 |
| [ADR-023](ADR-023-communications-as-authoritative-domain.md) | Communications as an authoritative communication-metadata domain | Accepted | Domain (communications) | D.18 |
| [ADR-024](ADR-024-scheduling-as-authoritative-domain.md) | Scheduling as an authoritative scheduling-metadata domain | Accepted | Domain (scheduling) | D.19 |
| [ADR-025](ADR-025-operations-as-authoritative-firm-domain.md) | Enterprise Operations as an authoritative firm-operations domain | Accepted | Domain (operations) | D.20 |
| [ADR-026](ADR-026-reporting-as-composition-layer.md) | Enterprise Reporting as a composition layer over Analytics | Accepted | Composition (reporting) | D.21 |
| [ADR-027](ADR-027-automation-as-orchestration-layer.md) | Enterprise Automation as an orchestration layer over the existing scheduler | Accepted | Domain (automation) | D.22 |
| [ADR-028](ADR-028-governance-reuses-matching-merge-retention.md) | Data Governance reuses matching/merge/retention; never an unsafe merge or hard delete | Accepted | Domain (governance) | D.23 |
| [ADR-029](ADR-029-integration-reuses-importers-outbox-crypto.md) | Enterprise Integration reuses importers/OAuth/outbox/crypto; metadata-only, no broker, no plaintext secrets | Accepted | Domain (integration) | D.24 |
| [ADR-030](ADR-030-security-metadata-domain-reuses-auth-crypto-audit.md) | Enterprise Security reuses auth/RBAC/crypto/audit; metadata-only, never replaces login/OAuth, never plaintext secrets | Accepted | Domain (security) | D.25 |
| [ADR-031](ADR-031-observability-metadata-domain-reuses-health-scheduler-logging.md) | Enterprise Observability reuses health/scheduler/logging/notifications; metadata-only, never replaces runtime health/logging/exception handling | Accepted | Domain (observability) | D.26 |
| [ADR-032](ADR-032-configuration-metadata-domain-reuses-runtime-config.md) | Enterprise Configuration reuses the runtime config/env; metadata-only, no runtime feature-toggle engine, never replaces runtime configuration | Accepted | Domain (configuration) | D.27 |
| [ADR-033](ADR-033-runtime-configuration-engine.md) | Runtime Configuration Engine: deterministic evaluation over D.27 metadata, immutable snapshots, in-process cache; never edits metadata, never blocks startup | Accepted | Runtime (configuration) | D.28 |
| [ADR-034](ADR-034-distributed-runtime-coordination.md) | Distributed Runtime Coordination: cluster-safe convergence over the transactional outbox (sole bus); the persisted generation is the single source of truth | Accepted | Runtime (coordination) | D.29 |
| [ADR-035](ADR-035-runtime-consumption-layer.md) | Runtime Consumption Layer: application behavior consumes the engine through a standardized, behavior-preserving API; infrastructure stays a startup concern | Accepted | Runtime (consumption) | D.30 |
| [ADR-036](ADR-036-runtime-authority-and-legacy-retirement.md) | Runtime Authority: the engine is authoritative for migrated behavior; legacy fallbacks retired to documented compatibility shims; runtime metadata governed | Accepted | Runtime (authority) | D.31 |
| [ADR-037](ADR-037-runtime-policy-engine.md) | Runtime Policy Engine: centralized, declarative business decisions that consume RuntimeContext (the engine stays the sole evaluator); a governed policy registry; policies never bypass RBAC | Accepted | Runtime (policy) | D.32 |
| [ADR-038](ADR-038-workflow-orchestration-engine.md) | Workflow Orchestration Engine: centralized, declarative, deterministic process coordination that consumes RuntimeContext + the Policy Engine; deterministic replay + simulation; a governed workflow registry; coordinates existing services, never duplicating domain behavior | Accepted | Orchestration (workflow) | D.33 |
| [ADR-039](ADR-039-domain-event-model.md) | Enterprise Domain Event Model: a typed, versioned, governed domain-event layer over the existing transactional outbox (the sole bus); no second event table; orchestration publishes domain events; reuses delivery guarantees / dead-letter / versioning | Accepted | Events (messaging) | D.34 |
| [ADR-040](ADR-040-domain-event-producer-adoption.md) | Domain Event Producer Adoption: the major business domains publish completed, references-only business facts (not commands) through the existing model; additive + behavior-preserving; authoritative writes unchanged; consumers never become sources of truth | Accepted | Events (adoption) | D.35 |
| [ADR-041](ADR-041-read-models-and-projections.md) | Read Models & Projection Engine: disposable, event-derived read models built from the outbox; the write side stays authoritative; replay rebuilds projections deterministically; projections never contain business rules or read authoritative tables | Accepted | Read models (projections) | D.36 |
| [ADR-042](ADR-042-read-surface-adoption.md) | Read Surface Adoption: adopt projections into read surfaces incrementally with graceful fallback; served only on the firm-wide path (RBAC preserved); the write side stays authoritative; behavior unchanged until projections are enabled + rebuilt | Accepted | Read models (adoption) | D.37 |
| [ADR-043](ADR-043-advisor-workspace-home.md) | Advisor Workspace Home: a personalized, projection-backed advisor home extending `/workspace`; a 12-widget grid (reorder/hide/pin/presets) + TODAY + PRIORITIES + AI-ready summary models; view-state only (no business mutation), RBAC preserved, behavior unchanged by default | Accepted | Advisor experience (workspace) | D.38 |
| [ADR-044](ADR-044-unified-work-queue.md) | Unified Work Queue: a cross-domain composition surface at `/work` normalizing actionable work from 10 authoritative services; not a second task/workflow/exception/assignment engine; actions delegate to the owning service; saved views are presentation state; no new projection; RBAC/record-scope preserved, fail closed | Accepted | Work execution (queue) | D.39 |
| [ADR-045](ADR-045-client360-workspace.md) | Client 360 Workspace: the master client record at `/client/{id}` composing 12 domain sections read-only; not a second client database; record scope verified once at the boundary; every edit deep-links to the authoritative workflow; unmodelled concepts (net worth, etc.) surfaced as "not tracked", never fabricated; no migration, no new projection | Accepted | Client record (workspace) | D.40 |
| [ADR-046](ADR-046-household360-workspace.md) | Household 360 Workspace: upgrades `/client/household/{id}` into a full household composition — member directory + member-level rollups + household relationship graph + snapshot; member visibility via `accessible_person_ids` (fail closed); reuses the single portfolio aggregation (never re-summed) and D.39 for work; no fabricated net worth, no inferred filing/dependency; reciprocal person↔household nav; no migration/table/projection/capability | Accepted | Client record (household) | D.41 |
| [ADR-047](ADR-047-advisor-ai-assist.md) | Advisor AI Assist: grounded, READ-ONLY briefing intelligence consuming the D.38–D.41 summaries; deterministic offline provider (no LLM infra); structured grounding + internal citations + limitations + "Review Required" human-review; refuses regulated requests; never mutates/writes DB/publishes outbox; no autonomous action, deep-links only; no migration/table/projection/capability | Accepted | Advisor experience (AI assist) | D.42 |
| [ADR-048](ADR-048-secure-client-portal.md) | Secure Client & Household Portal: hardens the EXISTING portal (no second system) with a declarative field-level visibility registry, runtime + compliance production gates (external access OFF and BLOCKED until a compliance reviewer is recorded), consent / electronic-delivery ledger (one new table `portal_consents`), masked/gated/fail-closed financial summary, offline production-guarded identity provider, internal-only diagnostics + governance, appointment-request delegation via secure messaging, and a capability-guarded record-scoped internal admin surface; no new outbox contract, no new RBAC capability, single migration head | Accepted | Client experience (portal) | D.43 |
| [ADR-049](ADR-049-unified-communications.md) | Unified Communications & Client Engagement: a governed COMPOSITION over the authoritative communication subsystems (no second messaging/notification/timeline/document/scheduling store, no copied content) — a declarative interaction registry that also classifies authoritative timeline events, a normalized reference-only interaction model, read-only fail-closed adapters (advisor spine delegates to `activity_timeline`; client spine reuses D.43 portal reads), unified engagement timeline + search + summary, Client 360 / Household 360 Communications sections, AI grounding (counts only), low-cardinality analytics, internal diagnostics, governance, runtime gates; no migration/table/capability/outbox contract | Accepted | Client experience (communications) | D.44 |
| [ADR-050](ADR-050-enterprise-knowledge-graph.md) | Enterprise Knowledge Graph & Explainable Relationship Layer: a governed SEMANTIC COMPOSITION over the authoritative relationship engine + entities — **not** a graph database (no Neo4j/RDF/SPARQL), **not** a second relationship engine, no duplicate entity/relationship tables; declarative entity + relationship registries, reference-only node/edge/path/explanation models, read-only fail-closed adapters (relationship adapter composes `build_relationship_graph` + `accessible_person_ids` scope filter; advisor adapter over `record_assignments`; domain adapter as bounded collection nodes), bounded cycle-safe scope-enforced traversal, explainability engine (why/owner/evidence/deep-link/inferred-vs-authoritative), semantic search, Client 360 / Household 360 Knowledge sections, AI grounding (counts only), analytics/diagnostics/governance, runtime + policy gates; no migration/table/capability/outbox contract | Accepted | Client experience (knowledge) | D.45 |
| [ADR-051](ADR-051-operational-intelligence.md) | Enterprise Operational Intelligence & Explainable Recommendation Layer: a governed READ-ONLY composition over the platform's existing authoritative recommendation sources (the deterministic `advisor_intelligence` Signal engine + the pipeline/bizdev/firm observation sets + the unified work queue + the D.44 engagement summary) — **not** a second recommendation/workflow/opportunity/CRM/analytics/AI engine, **no ML / predictive / black-box scoring**, no new persistence; a declarative recommendation registry (owner/source/severity/category/lifecycle/prereqs/visibility/explanation-template/evidence/deep-link/workflow-owner/suppression) + deterministic Signal classifier, a normalized explainable Recommendation model (`is_explainable` = why + evidence + deep link, hard emit gate), read-only fail-closed adapters, engine with dedup/suppression/prioritization/household-aggregation, Advisor Workspace panel + Client 360 / Household 360 Recommendations sections + AI summarize-only grounding, analytics/diagnostics/governance, runtime + policy gates; deterministic rule-based (confidence 1.0/source-supplied) rather than opaque ML; no migration/table/capability/outbox contract | Accepted | Advisor experience (recommendations) | D.46 |
| [ADR-052](ADR-052-compliance-intelligence.md) | Enterprise Compliance Intelligence & Supervisory Operations: a governed READ-ONLY supervisory composition over the authoritative compliance/review/exception/audit/approval/licensing services — **not** a second compliance rules engine, approval engine, audit log, or workflow, no mutation; strict separation of **supervision** (this layer observes/explains/prioritizes) from **execution** (the authoritative engines own every mutation); two declarative registries (12 supervisory review types + 10 exception types), explainable SupervisoryItem/ComplianceException models, read-only fail-closed adapters over `compliance.reviews` / `exception_engine` / `insurance_licensing` / portfolio cadence, supervisory dashboard + Client 360 / Household 360 Compliance-Oversight sections + Advisor Workspace advisor-tasks (governed-only) + AI summarize-only; the supervisor-vs-advisor boundary is an explicit new read-only capability `compliance.supervise` (sensitive; administrator + compliance, NOT advisor) so supervisory findings never leak to advisors/clients; one migration seeds only the capability (no table), single head | Accepted | Compliance / supervision | D.47 |
| [ADR-053](ADR-053-executive-reporting.md) | Enterprise Reporting & Executive Intelligence: a governed READ-ONLY executive-dashboard composition over the authoritative operational services + the SINGLE Analytics Registry — **not** a second analytics engine, data warehouse, BI platform, reporting database, ETL layer, or metrics system, **no new metrics, no persistence, no mutation, no copied operational data**; two declarative registries (8 dashboards + 14 widgets), explainable WidgetResult/Dashboard models, a widget compute layer where KPI values flow through `analytics.metrics.compute_metric` (inheriting record scope + the `analytics.executive` gate) and firm reads come from the authoritative firm services; executive engine with dashboard-level authorization (executive dashboards need `analytics.executive`; non-executives get 404 + restricted widgets, values never leaked); Advisor Workspace Executive Insights panel + Client 360 / Household 360 Executive sections + AI summarize-only; four operational counters registered into the ONE Analytics Registry (no second registry); reuses `analytics.view` / `analytics.executive` — no new capability/metric/migration, single head | Accepted | Firm intelligence (executive reporting) | D.48 |
| [ADR-054](ADR-054-practice-management.md) | Enterprise Practice Management & Capacity Planning: a governed READ-ONLY composition over the authoritative operational owners — Operations Capacity (the D.20 capacity/utilization owner), the Unified Work Queue, Workflow Automation, Work Management, Scheduling, the opportunity + Analytics firm-intelligence layers, and the tax domain — **not** a second workflow engine, scheduler, staffing/assignment engine, work queue, capacity/planning engine, or metrics registry, **no new metrics, no persistence, no mutation**; four declarative registries (9 capacity models + 6 resource classes + 19 panels + 8 dashboards), explainable PanelResult/PracticeDashboard models, a fail-closed panel compute layer where every utilization figure comes from `operations.capacity` and every workload/backlog/SLA count from the authoritative reads; engine with dashboard authorization (`capacity.read`) + per-panel self-restriction (value never leaked); Advisor Workspace Capacity Planning panel + Client 360 / Household 360 operational-workload sections + an Executive Practice Management dashboard (reusing existing widgets) + AI summarize-only (never assigns/rebalances/reschedules); four operational counters registered into the ONE Analytics Registry; reuses `capacity.read` / `work.read` / `analytics.view` / `operations.view` — no new capability/metric/migration, single head | Accepted | Firm operations (practice management) | D.49 |
| [ADR-055](ADR-055-document-intelligence.md) | Enterprise Document Intelligence & Records Lifecycle: a governed READ-ONLY composition over the authoritative document systems — the Document Platform (D.16, the single document + metadata + folder + version + lifecycle + retention-policy owner), Governance retention (D.23, records retention / legal holds / disposition), and Compliance Intelligence (D.47, documentation gaps normalized from the authoritative exception engine) — **not** a second DMS, OCR engine, indexing/search engine, archive, document database, metadata store, or records repository, **no new metrics, no persistence, no OCR, no index, no mutation**; three declarative registries (10 document classes + 6 retention policies + 18 panels + 6 dashboards), explainable PanelResult/IntelligenceDashboard models carrying counts + status only (never document content), a fail-closed panel compute layer where inventory/lifecycle/OCR-status come from the Document Platform, retention/archive/disposition from Governance retention, and missing-documentation from Compliance Intelligence; engine with dashboard authorization (`documents.view`) + per-panel self-restriction (value never leaked); Advisor Workspace Document Intelligence panel + Client 360 / Household 360 document sections + an Executive Document Intelligence dashboard (reusing existing widgets) + AI summarize-only (never alters metadata/archives/deletes/retention/ownership); four operational counters registered into the ONE Analytics Registry; the OCR-status panel reports the platform's own `ocr_status` and runs no OCR; reuses `documents.view` — no new capability/metric/migration, single head | Accepted | Records & information governance | D.50 |
| [ADR-056](ADR-056-automation-orchestration.md) | Enterprise Automation Orchestration & Business Process Composition: a governed READ-ONLY composition over the authoritative operational services — the Workflow Engine (`workflow_automation` + the `workflow_orchestration` facade), the Automation scheduled-job engine (ADR-027), the Trigger engine + action catalog, the Event outbox, Scheduling, and Communications — **not** a second workflow engine, scheduler, rules engine, orchestration engine, event bus, or automation platform, **no new metrics, no persistence, no mutation**; three declarative registries (9 automations + 7 trigger types + 6 actions) + 17 panels + 6 dashboards, explainable PanelResult/OrchestrationDashboard models carrying counts + status only (never a workflow payload), a fail-closed panel compute layer where workflow status comes from the Workflow Engine, jobs/runs from the Automation engine, triggers from the Trigger engine, events from the outbox, and scheduling/notifications from Scheduling/Communications; engine with dashboard authorization (`automation.view`) + per-panel self-restriction (value never leaked); Advisor Workspace Automation Status panel + Client 360 / Household 360 Automation History sections + an Executive Automation dashboard (reusing existing widgets) + AI summarize-only (never executes/approves/triggers/alters workflow state); four operational counters registered into the ONE Analytics Registry; reuses `automation.view` — no new capability/metric/migration, single head | Accepted | Firm operations (automation) | D.51 |
| [ADR-057](ADR-057-data-governance.md) | Enterprise Data Governance, Master Data & Platform Stewardship: a governed READ-ONLY composition over the authoritative data owners — the D.23 Governance package (catalog metadata, quality validation, MDM duplicate/lineage, retention cases, overview), the Person-merge / entity-resolution engine, the Event registry, and the domain entity owners — **not** a second master-data platform, identity system, synchronization engine, entity-resolution engine, metadata repository, or merge engine, **no new metrics, no persistence, no mutation**; two declarative registries (15 governed entities + 8 stewardship roles) + 19 panels + 7 dashboards, explainable PanelResult/GovernanceDashboard models carrying counts + status only (never a client-sensitive payload), a fail-closed panel compute layer where metadata comes from the catalog, duplicates/lineage from MDM (composing the authoritative person-merge — never re-implemented), validation from the quality engine, and event lineage from the Event registry; engine with dashboard authorization (`governance.view`) + per-panel self-restriction (value never leaked); Advisor Workspace Data Governance panel + Client 360 / Household 360 Data Governance sections + an Executive Data Governance dashboard (reusing existing widgets) + AI summarize-only (never merges/alters identities/modifies metadata/approves stewardship/changes ownership/bypasses validation); four operational counters registered into the ONE Analytics Registry; reuses `governance.view` — no new capability/metric/migration, single head | Accepted | Data governance & stewardship | D.52 |
| [ADR-058](ADR-058-integration-hub.md) | Enterprise Integration Hub & Connected Platform Governance: a governed READ-ONLY composition over the authoritative integration owners — the D.24 Integration Platform (service / sync / connectors / webhooks / api / events), the Event outbox + Event registry, and the M365 / insurance / signature connectors — **not** a second integration platform, ESB, API gateway, synchronization engine, webhook processor, message broker, or event bus, **no new metrics, no persistence, no mutation, no outbound HTTP**; two declarative registries (18 connected platforms + 9 connectors) + 19 panels + 7 dashboards, explainable PanelResult/IntegrationDashboard models carrying counts + status only (never a secret/token/credential/payload), a fail-closed panel compute layer where integration/sync/auth/webhook/connector/API come from the Integration Platform and event routing from the Event outbox; engine with dashboard authorization (`integration.view`) + per-panel self-restriction (value never leaked); Advisor Workspace Integration Health panel + Client 360 / Household 360 External Integrations sections + an Executive Integration Health dashboard (reusing existing widgets) + AI summarize-only (never reconnects/refreshes tokens/triggers sync/invokes mutations/bypasses auth/changes settings); four operational counters registered into the ONE Analytics Registry; reuses `integration.view` — no new capability/metric/migration, single head | Accepted | Integrations & connected platforms | D.53 |
| [ADR-059](ADR-059-security-operations.md) | Enterprise Security Operations & Identity Governance: a governed READ-ONLY composition over the authoritative security owners — the Security metadata domain (service / providers / policies / incidents), the Identity owner (`identity.list_identity_data`), the RBAC foundation, and the hash-chain audit log (`audit_export`) — **not** a second IAM platform, identity provider, RBAC engine, authentication system, authorization engine, MFA provider, audit-logging platform, or SIEM, **no new metrics, no persistence, no mutation, no duplicated identities**; two declarative registries (6 identity classes + 6 security domains) + 21 panels + 7 dashboards, explainable PanelResult/SecurityDashboard models carrying counts + status only (never a password/secret/token/session-ID/payload), a fail-closed panel compute layer where authentication/policies/providers/incidents come from the Security domain, identity/RBAC from Identity, MFA from the users.mfa_enabled flag via Analytics, and sessions/audit from the hash-chain audit log; engine with dashboard authorization (`security.view`) + per-panel self-restriction (value never leaked; audit panels additionally require `audit.read`); Advisor Workspace Security Operations panel + Client 360 / Household 360 Security & Access sections + an Executive Security Operations dashboard (reusing existing widgets) + AI summarize-only (never authenticates/authorizes/elevates/issues tokens/resets passwords/disables MFA/bypasses security); four operational counters registered into the ONE Analytics Registry; reuses `security.view` / `audit.read` — no new capability/metric/migration, single head | Accepted | Security & identity governance | D.54 |
| [ADR-060](ADR-060-business-continuity.md) | Enterprise Business Continuity & Operational Resilience: a governed READ-ONLY composition over the authoritative operational-resilience owners — the Observability domain (service / catalog / health / incidents / alerts), the Runtime engine (service / coordination / consumption), the Automation scheduler, and Communications — **not** a second backup platform, monitoring system, disaster-recovery engine, scheduler, notification system, or incident manager, **no new metrics, no persistence, no mutation**; two declarative registries (9 resilience domains + 8 recovery assets) + 22 panels + 8 dashboards, explainable PanelResult/ContinuityDashboard models carrying counts + status only (never an infrastructure payload), a fail-closed panel compute layer where infrastructure/health/incidents/maintenance come from Observability, readiness/cluster/adoption from Runtime, scheduled jobs from Automation, and notifications from Communications; **backup / restore / DR have no authoritative owner in the platform — those panels report `not_configured` honestly, never a fabricated status** (the D.50/OCR precedent); engine with dashboard authorization (`observability.view`) + per-panel self-restriction (value never leaked); Advisor Workspace Operational Resilience panel + Client 360 / Household 360 Business Continuity sections + an Executive Operational Resilience dashboard (reusing existing widgets) + AI summarize-only (never starts backups/restores/acknowledges incidents/changes monitoring/alters runtime/modifies infrastructure); four operational counters registered into the ONE Analytics Registry; reuses `observability.view` — no new capability/metric/migration, single head | Accepted | Business continuity & resilience | D.55 |
| [ADR-061](ADR-061-vendor-management.md) | Enterprise Vendor Management & Third-Party Technology Governance: a governed READ-ONLY composition over the authoritative vendor / technology owners — the Integration Platform provider registry (`integration.connectors`, the vendor inventory of record), the Security certificate & secret store (`security.secrets`), the Observability service catalog, Insurance licensing, and Security incidents + Compliance Intelligence (third-party risk) — **not** a second vendor-management platform, procurement system, contract repository, CMDB, asset inventory, licensing platform, or risk engine, **no new metrics, no persistence, no mutation, no duplicated inventories**; two declarative registries (8 vendor classes + 8 technology-lifecycle classes) + 20 panels + 7 dashboards, explainable PanelResult/VendorDashboard models carrying counts + status only (never a contract/credential/license-key/secret/procurement payload), a fail-closed panel compute layer where the vendor inventory comes from the Integration Platform providers, licensing/renewal from the Security certificate store + Insurance licensing, lifecycle from the Observability catalog, and third-party risk from Security incidents + Compliance Intelligence; **procurement / contracts / subscriptions have no authoritative owner — declared `not_configured`, never a fabricated inventory** (the D.55 precedent); engine with dashboard authorization (`integration.view`) + per-panel self-restriction (risk panels require `security.view`; value never leaked); Advisor Workspace Technology & Vendor Health panel + Client 360 / Household 360 Technology Dependencies sections + an Executive Technology Governance dashboard (reusing existing widgets) + AI summarize-only (never approves purchases/renews contracts/terminates vendors/alters licensing/modifies subscriptions); four operational counters registered into the ONE Analytics Registry; reuses `integration.view` / `security.view` — no new capability/metric/migration, single head | Accepted | Vendor & technology governance | D.56 |
| [ADR-062](ADR-062-financial-operations.md) | Enterprise Financial Operations & Revenue Intelligence: a governed READ-ONLY composition over the authoritative financial owners — the insurance commission ledger (`insurance_reporting.commission_report`, the one money owner), the portfolio AUM owner, the single Analytics Registry revenue metrics (`analytics.metrics` / `analytics.trends`), Executive Reporting, and Practice Management — **not** a second accounting platform, ERP, billing engine, commission engine, payroll system, bookkeeping platform, general ledger, or budgeting application, **no new metrics, no persistence, no mutation, no duplicated accounting data**; two declarative registries (10 financial categories + 8 revenue types) + 20 panels + 7 dashboards (firm performance, revenue, profitability, expenses, payroll, commissions, financial operations), explainable PanelResult/FinancialDashboard models carrying firm-level aggregate totals + status only (never a payroll detail / tax return / bank account number / payment credential / accounting payload), a fail-closed panel compute layer where commissions come from the insurance commission ledger, AUM / business-development / pipeline revenue from the portfolio owner + the single Analytics Registry, firm KPIs from Executive Reporting, and operating efficiency from Practice Management; **billing / fee calculation / payroll / operating expenses / GL / profitability have no authoritative owner — declared `not_configured`, never a fabricated ledger** (the D.55 / D.56 precedent); engine with dashboard authorization (`analytics.view`) + per-panel self-restriction (firm financial figures require `analytics.executive`; value never leaked); Advisor Workspace Financial Performance panel + Client 360 / Household 360 Financial Relationship sections + an Executive Financial Operations dashboard (reusing existing widgets) + AI summarize-only (never issues invoices / processes payroll / modifies accounting records / changes commissions / alters billing / executes payments); four operational counters registered into the ONE Analytics Registry; reuses `analytics.view` / `analytics.executive` — no new capability/metric/migration, single head | Accepted | Financial operations & revenue intelligence | D.57 |
| [ADR-063](ADR-063-enterprise-risk-management.md) | Enterprise Risk Management, Internal Controls & Assurance Governance: a governed READ-ONLY composition over the authoritative risk / control / assurance owners — Compliance Intelligence + the Exception Engine (the single authoritative exception owner), Security Operations + incidents, Data Governance, the Integration Platform, Business Continuity, Vendor Management, Financial Operations, Document Intelligence, Automation Orchestration, Insurance licensing, and the Runtime + Policy engines + audit logging — **not** a second GRC platform, risk register, compliance engine, exception system, audit platform, incident-management system, control-testing application, policy engine, or approval engine, **no new capability, no new metric, no persistence, no mutation, no duplicated risk data**; three declarative registries (15 risk domains + 20 control families + 15 assurance sources) + 24 panels + 8 dashboards, explainable PanelResult/RiskDashboard models carrying counts / status / severity distributions / coverage summaries only (never client-sensitive evidence / audit payloads / security details / credentials / tokens / bank info / tax-return contents / document contents / private incident narratives), a fail-closed panel compute layer where every displayed severity/status comes from an authoritative source and the enterprise-risk-posture panel is a DERIVED coverage summary labeled `derived` (never a fabricated composite risk score or regulatory certification); **control testing / effectiveness, model/AI risk, privacy risk, financial authorization, and change management have no authoritative owner — declared `not_configured`, never fabricated**; engine with dashboard authorization (supervisor OR executive: `compliance.supervise` / `analytics.executive`) + per-panel self-restriction to the source capability (value + metadata never leaked); Advisor Workspace Enterprise Risk & Controls panel + Client 360 / Household 360 Risk & Controls sections (record-scoped, never firm-wide findings) + an Executive Enterprise Risk & Assurance dashboard (reusing existing widgets) + AI summarize-only (never assigns risk / changes severity / accepts risk / closes findings / certifies controls / approves exceptions / acknowledges incidents / assigns remediation / certifies compliance / invents evidence / infers regulatory approval); an absent finding never certifies compliance; four operational counters registered into the ONE Analytics Registry; reuses existing supervisory / executive / domain capabilities — no new capability/metric/migration, single head | Accepted | Enterprise risk, controls & assurance | D.58 |
| [ADR-064](ADR-064-regulatory-examination-readiness.md) | Enterprise Regulatory Examination Readiness, Evidence Governance & Supervisory Certification: a governed READ-ONLY composition over the authoritative regulatory / evidence / certification owners — Compliance Intelligence + `compliance/reviews` + the rule catalog + the reviewer-authority owner (`reviewer_authorities`, seeded EMPTY), the Exception Engine, Document Intelligence, Data Governance, Security Operations, Business Continuity, Vendor Management, Financial Operations, Insurance licensing, audit logging, and the CI pipeline — **not** a second compliance platform, examination-management system, audit platform, document repository, records-management system, regulatory filing system, certification engine, evidence vault, supervisory approval engine, or policy-management system, **no new capability, no new metric, no persistence, no mutation, no duplicated evidence**; four declarative registries (23 regulatory obligations + 27 evidence classes + 22 examination-request categories + 14 certification sign-offs) + 37 panels + 8 dashboards, explainable PanelResult/ReadinessDashboard models carrying counts / status / coverage / freshness / age bands only (never document contents / tax-return contents / client narratives / regulator correspondence / audit payloads / credentials / tokens / account numbers / license keys / PII / evidence files); **regulatory filing, examination-case ownership, certification reviewers, evidence export, backup/restore evidence, and several obligation domains (IA registration, Form ADV, advertising review, custody, conflicts, complaints) have no authoritative owner — declared `not_configured`, never fabricated**; **every certification is BLOCKED / `reviewer_not_confirmed` because reviewer authority is a recorded fact (seeded empty) that is never inferred, named reviewers + review dates are never fabricated, and business approval is never regulatory certification (Michael Shelton is the business owner, not the regulatory certifier unless a recorded reviewer_authorities record confirms it)**; the derived readiness panel is labeled `derived` and describes OPERATIONAL READINESS, never regulatory certification — there is no single "compliant" result and an absent finding is never compliance; engine with dashboard authorization (supervisor OR executive: `compliance.supervise` / `analytics.executive`) + per-panel self-restriction to the source capability (value / count / freshness / metadata never leaked); Advisor Workspace Regulatory Readiness panel + Client 360 / Household 360 Evidence & Supervisory Readiness sections (record-scoped, never firm-wide examination posture) + an Executive Regulatory Readiness & Evidence dashboard (reusing existing widgets) + AI summarize-only (never certifies compliance / claims regulator acceptance / approves a rule set / signs an attestation / infers reviewer authority / invents evidence / fabricates a filing acknowledgement / files a form / closes a finding / treats business approval as certification); four operational counters registered into the ONE Analytics Registry; reuses existing supervisory / executive / domain capabilities — no new capability/metric/migration, single head | Accepted | Regulatory readiness, evidence & certification | D.59 |
| [ADR-065](ADR-065-operational-resilience.md) | Enterprise Operational Resilience, Incident Management & Service Continuity Intelligence: a governed READ-ONLY composition over the authoritative operational-resilience owners — the Observability service catalog / health / incidents / alerts (service health, reliability incidents, alerts, **maintenance windows**), Security incidents, the Integration Platform, Vendor Management, Automation Orchestration, and Business Continuity — **not** a second incident-management platform, ticketing system, monitoring platform, help desk, disaster-recovery platform, change-management platform, CMDB, scheduler, or alerting engine, **no new capability, no new metric, no persistence, no mutation, no duplicated operational data**; five declarative registries (6 operational services + 7 incident categories + 7 continuity capabilities + 5 recovery objectives + 4 operational dependencies) + 24 panels + 8 dashboards, explainable PanelResult/ResilienceDashboard models carrying counts / status / coverage only (never a sensitive operational payload); **backup, restore, disaster recovery, recovery testing, failover, outage-history/uptime, and vendor incidents have no authoritative owner — declared `not_configured`, never fabricated** (maintenance windows + alerting ARE owned by Observability, so they are configured); the executive-operational-status panel is a DERIVED posture labeled `derived` that describes OPERATIONAL POSTURE, **never a certification that production is healthy or continuity assured** — an absent incident is not health; engine with dashboard authorization (operations OR executive: `observability.view` / `analytics.executive`) + per-panel self-restriction to the source capability (value / count never leaked); Advisor Workspace Operational Status panel + Client 360 / Household 360 Operational Impact sections (record-scoped Integration-Hub dependency impact only — firm-wide operational information is never exposed at client/household scope) + an Executive Enterprise Operational Resilience dashboard (reusing existing widgets) + AI summarize-only (never declares production healthy / certifies continuity / infers recovery success / fabricates incidents / generates alerts); four operational counters registered into the ONE Analytics Registry; reuses `observability.view` — no new capability/metric/migration, single head | Accepted | Operational resilience & incident intelligence | D.60 |
| [ADR-066](ADR-066-capacity-planning.md) | Enterprise Capacity Planning, Workforce Operations & Resource Intelligence: a governed READ-ONLY composition over the authoritative workforce / capacity / utilization owners — the Operations capacity owner (firm utilization / resources / capacity plans), the Work Queue (workload / backlog / queue health / assignments), Practice Management (staffing recommendations), and Automation Orchestration (automation-worker workload) — **not** a second HR platform, HCM, scheduling application, calendar system, project-management system, PSA, time-tracking platform, payroll platform, or workforce-management system, **no new capability, no new metric, no persistence, no mutation, no duplicated workforce data**; three declarative registries (8 workforce classes + 9 capacity categories + 5 utilization indicators) + 23 panels + 8 dashboards, explainable PanelResult/ResourceDashboard models carrying counts / status / coverage only (never an employee detail / payroll / HR record / calendar content / time entry / sensitive staffing datum); **a full HR employee directory, contractors, PTO / availability, time-tracking, payroll, and meeting / onboarding / planning capacity have no authoritative owner — declared `not_configured`, never fabricated**; the executive-workforce-status panel is a DERIVED operational summary labeled `derived` — never a certified staffing / utilization figure and never an HR record; engine with dashboard authorization (operations OR executive: `capacity.read` / `analytics.executive`) + per-panel self-restriction to the source capability (value / count never leaked); Advisor Workspace Capacity & Workload panel + Client 360 / Household 360 Servicing Team sections (record-scoped `resolve_assignments` who-services-the-record only — employee workload + firm utilization are never exposed at client/household scope) + an Executive Enterprise Workforce & Capacity dashboard (reusing existing widgets) + AI summarize-only (never assigns work / approves staffing / schedules employees / modifies assignments / infers availability / fabricates utilization); four operational counters registered into the ONE Analytics Registry; reuses `capacity.read` / `work.read` / `automation.view` — no new capability/metric/migration, single head | Accepted | Capacity planning & workforce operations | D.61 |
| [ADR-067](ADR-067-knowledge-management.md) | Enterprise Knowledge Management, SOP Governance & Institutional Intelligence: a governed READ-ONLY composition over the authoritative knowledge / SOP / documentation owners — the Document Platform (documents, classification, deterministic lifecycle, immutable versions, ownership), Document Intelligence (completeness / gaps / freshness), and Data Governance retention — **not** a second wiki, document-management platform, Confluence replacement, SharePoint, records-management platform, search engine, AI knowledge store, or document repository, **no new capability, no new metric, no persistence, no mutation, no duplicated documentation**; five declarative registries (8 knowledge domains + 6 SOP categories + 5 documentation owners + 7 knowledge sources + 5 publication statuses) + 22 panels + 8 dashboards, explainable PanelResult/KnowledgeDashboard models carrying counts / status / coverage only (never document contents / confidential procedures / credentials / tokens / client-sensitive documentation); **SOP governance, runbooks, playbooks, onboarding SOPs, a knowledge base, institutional memory, a wiki, Confluence, and a dedicated full-text / vector search index have no authoritative owner — declared `not_configured`, never fabricated**; the master runtime gate is `knowledge_management.enabled` (distinct from the D.45 knowledge GRAPH's `knowledge.enabled`); the executive-knowledge-status panel is a DERIVED documentation-coverage summary labeled `derived` — never a certified SOP / approval / institutional-knowledge figure; engine with dashboard authorization (documentation OR executive: `documents.view` / `analytics.executive`) + per-panel self-restriction to the source capability (value / count never leaked); Advisor Workspace Knowledge & SOPs panel + Client 360 / Household 360 Documentation sections (record-scoped `client_documents` only — internal SOPs + firm-wide documentation metrics are never exposed at client/household scope) + an Executive Enterprise Knowledge & Documentation dashboard (reusing existing widgets) + AI summarize-only (never invents documentation / fabricates SOPs / creates procedures / implies approvals / infers unpublished knowledge / modifies documentation); four operational counters registered into the ONE Analytics Registry; reuses `documents.view` / `governance.view` — no new capability/metric/migration, single head | Accepted | Knowledge management, SOP governance & institutional intelligence | D.62 |
| [ADR-068](ADR-068-change-management.md) | Enterprise Change Management, Release Governance & Configuration Intelligence: a governed READ-ONLY composition over the authoritative change / release / configuration / evidence owners — the architecture manifest (declared release line / migration head / route + capability counts), Observability health (the **live** Alembic script head, `len(app.routes)`, the ADR glob, and the live section / dashboard registries — self-verifiable declared-vs-live drift), the Runtime + Policy engines (flags / policy coverage), the Observability catalog / alerts / incidents (deployment references / maintenance / incidents), Security incidents, Compliance Intelligence (change-related exceptions), and the CI pipeline evidence the manifest records (build / E2E / regression / code-quality / architecture-guard / documentation-advisory — referenced per-commit, not live-read) — **not** a second ITSM, change-management, deployment, CI/CD, Git, CMDB, feature-flag, release-approval, incident, or maintenance-scheduling platform, **no new capability, no new metric, no persistence, no mutation, no duplicated change data**; four declarative registries (15 change domains + 15 release entries + 13 configuration entries + 20 change-evidence entries) + 35 panels + 8 dashboards, explainable PanelResult/ChangeDashboard models carrying counts / status / identifiers / hashes / timestamps / coverage / verification only (never a credential / secret / token / environment variable / connection string / private key / deployment payload / protected infrastructure detail / sensitive configuration value / private incident narrative); **live git / PR / branch / merge-commit / version-tag / CI status, deployment execution / status, rollback readiness, production verification, change calendar, and post-change review have no authoritative owner — declared `not_configured`, never fabricated**; distinct master gates `change_management.enabled` / `release_governance.enabled` / `configuration_intelligence.enabled` / `deployment_evidence.enabled` / `change_ai_summary.enabled` (no reused/unrelated gate, no env fallback); the executive-change-posture + derived-change-readiness panels are DERIVED operational-readiness summaries labeled `derived` — **a green build is not production certification, a merged pull request is not deployment, a version tag does not prove rollout, a clean migration check does not prove application health, an absent incident is not change success**; engine with dashboard authorization (operations OR executive: `observability.view` / `analytics.executive`) + per-panel self-restriction to the source capability (value / count never leaked); Advisor Workspace Change & Release Status panel + Client 360 / Household 360 Change Impact sections (record-scoped affected-integration surface only — firm-wide change / release / deployment / CI status is never exposed at client/household scope) + an Executive Enterprise Change & Release Governance dashboard (reusing existing widgets) + AI summarize-only (never creates a branch / merges / deploys / runs a migration / changes a flag / approves / rolls back / certifies production / implies a deployment); four operational counters registered into the ONE Analytics Registry; reuses `observability.view` / `analytics.executive` / `security.view` / `compliance.supervise` — no new capability/metric/migration, single head | Accepted | Change management, release governance & configuration intelligence | D.63 |
| [ADR-069](ADR-069-environment-management.md) | Enterprise Environment Management, Deployment Topology & Platform Lifecycle Intelligence: a governed READ-ONLY composition over the authoritative environment / platform / deployment-topology / lifecycle / infrastructure-dependency owners — the **Observability catalog** (environment profiles, deployment references, service inventory, the service dependency graph), the **Observability health** owner (runtime snapshots, the live Alembic script head), the **Observability service** overview, the **Runtime + Policy engines** (configuration coverage), and the **Integration platform** (integration dependencies) — **not** a second CMDB, infrastructure-management platform, cloud-management platform, deployment orchestrator, asset inventory, configuration database, environment manager, or monitoring platform, **no new capability, no new metric, no persistence, no mutation, no duplicated environment / infrastructure data**; five declarative registries (8 environment + 9 platform + 7 deployment-topology + 8 lifecycle + 7 infrastructure-dependency domains) + 35 panels + 8 dashboards, explainable PanelResult/EnvironmentDashboard models carrying counts / status / identifiers / coverage / verification only (never a credential / secret / token / environment variable / connection string / private key / deployment payload / protected infrastructure detail / private topology / sensitive configuration value); **cloud resources, servers, containers, VMs, formal lifecycle state, deprecation, retirement records, decommission schedule, host / network topology, and live deployment execution have no authoritative owner — declared `not_configured`, never fabricated**; distinct master gates `environment_management.enabled` / `platform_lifecycle.enabled` / `deployment_topology.enabled` / `environment_ai_summary.enabled` (no reused/unrelated gate, no env fallback); the executive-platform-posture + lifecycle-readiness panels are DERIVED operational-visibility summaries labeled `derived` — **environment metadata is not live infrastructure, a deployment reference is not a deployment, an active flag is not a lifecycle guarantee, and a runtime snapshot is not continuous environment health**; engine with dashboard authorization (operations OR executive: `observability.view` / `analytics.executive`) + per-panel self-restriction to the source capability (value / count never leaked); Advisor Workspace Environment & Platform Status panel + Client 360 / Household 360 Platform Dependencies sections (**reported `not_configured` honestly — no authoritative record-scoped platform / environment / infrastructure owner exists, platform impact is never inferred, internal infrastructure is never exposed**) + an Executive Enterprise Platform & Environment Landscape dashboard (reusing existing widgets) + AI summarize-only (never invents environments / fabricates infrastructure / infers deployments / certifies platform health / modifies topology / provisions resources); four operational counters registered into the ONE Analytics Registry; reuses `observability.view` / `analytics.executive` / `integration.view` — no new capability/metric/migration, single head | Accepted | Environment management, deployment topology & platform lifecycle intelligence | D.64 |
| [ADR-070](ADR-070-identity-governance.md) | Enterprise Identity, Access Governance & Authorization Intelligence: a governed READ-ONLY composition over the authoritative identity / role / capability / authentication / authorization owners — the **Identity service** (`list_identity_data` — the user / role / capability / team directory), **Security RBAC** (role & capability resolution, authorization policies, the authorization event ledger), **Security Authentication** (providers), the **Policy engine** (policy coverage), and **Security Authorization** (record-scope decisions) — **not** a second identity provider, authentication service, authorization engine, RBAC system, directory, SSO platform, policy engine, or user-management platform, **no new capability, no new metric, no persistence, no mutation, no duplicated identity / authorization data**; five declarative registries (8 identity + 7 role + 6 capability + 7 authentication + 7 authorization domains) + 33 panels + 8 dashboards, explainable PanelResult/IdentityDashboard models carrying counts / coverage / status / ratios only (never a password / secret / token / session ID / credential / authentication payload / raw identity / privileged-role membership / user-level permission map); **SSO / external IdP, MFA enforcement, service accounts, API-key / token identities, access reviews, PAM, segregation of duties, identity lifecycle, and password management have no authoritative owner — declared `not_configured`, never fabricated**; distinct master gates `identity_governance.enabled` / `authentication_landscape.enabled` / `authorization_landscape.enabled` / `identity_ai_summary.enabled` (no reused/unrelated gate, no runtime-variable bypass); the executive-identity-posture + access-governance-readiness panels are DERIVED governance-readiness summaries labeled `derived` — **a capability inventory is not a grant, a role definition is not an assignment, a provider registration is not an authentication, and coverage is not certification**; engine with dashboard authorization (identity-admin OR executive: `identity.manage` / `analytics.executive`) + per-panel self-restriction to the source capability (value / count never leaked); Advisor Workspace Identity & Access Status panel + Client 360 / Household 360 Authorization Context sections (**expose ONLY the current principal's OWN record-scope authorization decision via `record_in_scope` — never another identity, a privileged role, or a permission map, and authorization is never inferred**) + an Executive Enterprise Identity & Access Governance dashboard (reusing existing widgets) + AI summarize-only (never authenticates / authorizes / assigns roles / recommends privilege escalation / fabricates permissions / invents identities / bypasses policy); four operational counters registered into the ONE Analytics Registry; reuses `identity.manage` / `analytics.executive` — no new capability/metric/migration, single head | Accepted | Identity, access governance & authorization intelligence | D.65 |
| [ADR-071](ADR-071-data-governance-intelligence.md) | Enterprise Data Governance, Lineage & Information Stewardship Intelligence: a governed READ-ONLY composition over the authoritative D.23 Governance owners — the **Governance catalog** (data domains, elements, quality rules, survivorship rules, stewardship via `steward_user_id`), **Governance MDM** (lineage & provenance, merge candidates), **Governance Quality** (findings), and **Governance Retention** (assignments, legal holds, deletion requests, cases) — **not** a second data catalog, metadata repository, ETL platform, MDM platform, warehouse, governance platform, lineage engine, or quality engine, **no new capability, no new metric, no persistence, no mutation, no duplicated metadata / lineage / governance data**; five declarative registries (8 data-domain + 5 lineage + 5 stewardship + 5 quality + 6 retention domains) + 29 panels + 8 dashboards, explainable PanelResult/DataGovernanceDashboard models carrying counts / coverage / status / ratios only (never a sensitive data value / client PII / credential / secret / token / confidential metadata / internal governance note / quality-rule internal); **external data catalog, business glossary, data classification, automated column-level lineage, data-sharing contracts, DQ scorecards / SLAs, a retention-policy catalog, and DPIA have no authoritative owner — declared `not_configured`, never fabricated**; distinct master gates `data_governance_intelligence.enabled` / `lineage_landscape.enabled` / `data_quality_landscape.enabled` / `data_governance_ai_summary.enabled` (distinct from the D.52 layer's `data_governance.enabled` / `lineage.enabled`, no reused/unrelated gate, no runtime-variable bypass); the executive-data-governance-posture + governance-readiness + data-risk panels are DERIVED governance-readiness summaries labeled `derived` — **a registered rule is not an executed check, a steward assignment is not a governance guarantee, a lineage record is not a complete lineage, and coverage is not certification**; engine with dashboard authorization (governance OR executive: `governance.view` / `analytics.executive`) + per-panel self-restriction to the source capability (value / count never leaked); Advisor Workspace Data Governance Status panel + Client 360 / Household 360 Data Lineage & Provenance sections (**expose ONLY record-scoped source-system provenance via `person_lineage` — never confidential metadata, quality-rule internals, or an inferred governance state**) + an Executive Enterprise Data Governance dashboard (reusing existing widgets) + AI summarize-only (never invents lineage / fabricates metadata / assigns stewardship / modifies governance / repairs data / infers missing ownership); four operational counters registered into the ONE Analytics Registry; distinct from (not a duplicate of) the D.52 Data Governance layer — both are read-only views over the single authoritative Governance package; reuses `governance.view` / `analytics.executive` — no new capability/metric/migration, single head | Accepted | Data governance, lineage & information stewardship intelligence | D.66 |

| [ADR-072](ADR-072-canonical-document-model.md) | Canonical document model: ONE canonical document (`documents.id`) that owns content hash, canonical local storage, OCR, AI extraction, classification, version history, audit, and retention; MANY source references (TaxDome, Drake, Schwab, AssetMark, Microsoft 365, manual upload, scanner, local repository) recorded as source references — today in `documents.tags`, later in a future additive `document_sources` table backfilled from those tags + `sha256` — never as separate documents; ownership (household, person(s), business, trust, estate) modeled as relationships **independent of source** and never as identity or a de-duplication key; OCR/AI/classification/version computed ONCE per canonical document and shared across sources; duplicate detection is content-hash-based (SHA-256) so a re-ingested copy from another system attaches a source reference instead of duplicating; documents the additive migration path (no identity rework, no duplicate-cleanup project) and confirms PR #168 alignment (canonical local storage already separated from source origin, hash on every row, ownership-only linking, `--repair-links` coexists with `document_sources`); remaining future/additive work: the `document_sources` table + hash-based cross-source resolution + a `document_ownership` join for many-valued ownership | Accepted | Canonical document model | Docs |

Related: `docs/PLATFORM_ARCHITECTURE.md`, `docs/ADVISOR_WORKSPACE_ARCHITECTURE.md`,
`docs/platform_architecture_manifest.yaml`, `tests/test_platform_architecture.py`,
`tests/test_architecture_decision_records.py`.
